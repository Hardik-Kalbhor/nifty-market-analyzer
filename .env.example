# =============================================================================
# NIFTY Market Analyzer - Environment Variables
# =============================================================================

# (Recommended) AI Provider Keys for Multi-Agent Debate, LLM Analysis & Vision OCR
# At least one key is recommended for full multi-agent consensus and deep reflection.
GEMINI_API_KEY=
GROQ_API_KEY=

# (Optional) Port - Render automatically sets $PORT in production
# PORT=5001

# (Optional) Custom History Directory (defaults to ./history with /tmp fallback)
# HISTORY_DIR=
